[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]",
    "gspread",
    "pillow",
    "numpy",
    "fastapi-analytics>=1.2.5",
]
